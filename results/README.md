# Results

This directory is for compact reproducible outputs such as model-comparison summaries, feature-selection summaries, SHAP summaries and polarization-analysis tables. Large generated files are excluded from version control by `.gitignore`.
